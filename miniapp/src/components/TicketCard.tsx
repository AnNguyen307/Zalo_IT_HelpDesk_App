import type{Ticket}from"../types";import{categoryIcon,categoryLabel,priorityLabel,relativeTime,statusLabel}from"../lib/ui";import{Icon}from"./Icon";
export function TicketCard({ticket,onClick}:{ticket:Ticket;onClick:()=>void}){const icon=categoryIcon[ticket.category]||"other",escalated=ticket.aiAnalysis?.escalated;return <button className={`ticket-card ${ticket.sla?.overdue?"ticket-overdue":ticket.sla?.paused?"ticket-sla-paused":""}`} onClick={onClick}><span className={`ticket-category-icon category-${ticket.category}`}><Icon name={icon}/></span><span className="ticket-card-body"><span className="ticket-card-top"><span className="ticket-code">{ticket.code}</span><span className={`pill status-${ticket.status}`}>{statusLabel[ticket.status]}</span></span><strong>{ticket.title}</strong><span className="ticket-description">{ticket.description}</span><span className="ticket-card-footer"><span>{categoryLabel[ticket.category]||ticket.category}</span><span className={`priority priority-${ticket.priority}`}>{priorityLabel[ticket.priority]}</span>{ticket.sla?.paused&&<span className="sla-paused-mini">SLA tạm dừng</span>}{escalated&&<span className="escalated-mini"><Icon name="alert" size={13}/> Đã chuyển IT</span>}{ticket.attachmentCount?<span><Icon name="attachment" size={13}/> {ticket.attachmentCount}</span>:null}<time>{relativeTime(ticket.updatedAt)}</time></span></span><Icon name="arrow-right" className="ticket-card-arrow"/></button>}
