import{useApp}from"../context";import{formatDate}from"../lib/ui";
export function NotificationsPage(){const{notifications,markNotificationRead,markAllNotificationsRead,navigate}=useApp();return <><section className="page-title page-title-row"><div><span className="eyebrow">CẬP NHẬT HỆ THỐNG</span><h1>Thông báo</h1><p>Phản hồi, SLA và thay đổi trạng thái.</p></div>{notifications.some(n=>!n.readAt)&&<button className="text-button" onClick={markAllNotificationsRead}>Đọc tất cả</button>}</section><div className="notification-list">{notifications.map(n=><button key={n.id} className={n.readAt?"":"unread"} onClick={async()=>{await markNotificationRead(n.id);if(n.ticketId)navigate("detail",n.ticketId)}}><span className="notification-copy"><strong>{n.title}</strong><p>{n.body}</p><small>{formatDate(n.createdAt,true)}</small></span></button>)}</div>{!notifications.length&&<div className="empty-state"><h3>Chưa có thông báo</h3><p>Các cập nhật mới sẽ xuất hiện tại đây.</p></div>}</>}
